# Structured Schema Test Plan

Validate `structured` schema tables, keys, constraints, and metadata relationships.

Scope:
- `05_structured.sql`
- data sources, datasets, tables, columns, relationships, statistics, and tags
