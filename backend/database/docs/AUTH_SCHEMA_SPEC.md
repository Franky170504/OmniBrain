# Authentication Schema Specification

Describes the `auth` schema objects used for user authentication and authorization.

## Schema files

- `backend/database/schema/02_auth.sql`

## Main objects

- `auth.roles`
- `auth.users`

## Purpose

Support user identity, roles, and authorization rules in OmniBrain.
