# Database Naming Guide

Defines naming conventions for the OmniBrain database.

General rules:

- Use `snake_case` for tables, columns, indexes, and constraints
- Prefix schema objects with schema names where appropriate
- Use descriptive names for foreign keys and unique constraints
- Keep identifiers concise and consistent
