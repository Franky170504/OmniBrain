# Database Standards

Guidelines for developing and maintaining the OmniBrain database.

Standards:

- Keep schema scripts idempotent when possible
- Use explicit foreign keys and constraints
- Document schema changes
- Add tests for schema updates
- Maintain clear module separation by schema
